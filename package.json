{
  "name": "zephyra-consultora",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.7.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "convex:check": "node ops/convex-identity.mjs --check",
    "convex:sync": "node ops/convex-identity.mjs --sync"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.2.0",
    "turbo": "^2.10.7"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "unrs-resolver"
    ]
  }
}
